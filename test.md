1. 알고리즘/자료구조
1.1. 다익스트라 알고리즘에 대한 설명으로 옳지 않은 것은?
A) 다익스트라는 그래프에서 모든 노드의 최단 경로를 구하는 알고리즘이다.
B) 다익스트라는 음수 가중치를 허용하지 않는다.
C) 다익스트라는 탐색을 위해 우선순위 큐를 사용한다.
<!-- D) 다익스트라는 모든 경로를 다 계산하고 나서 결과를 출력한다. -->

1.2. 배열의 크기를 동적으로 조정할 수 있는 자료구조는 무엇인가?
A) 배열
<!-- B) 연결 리스트 -->
C) 스택
D) 해시 테이블

1.3. 이진 탐색 트리에서, 모든 왼쪽 자식 노드의 값은 부모보다 작고, 모든 오른쪽 자식 노드의 값은 부모보다 큰 특성을 가진 트리를 무엇이라 하는가?
A) 힙
B) AVL 트리
<!-- C) 이진 탐색 트리 -->
D) 레드-블랙 트리
2. 데이터베이스

2.1. 관계형 데이터베이스에서 데이터 중복을 최소화하고 일관성을 유지하는 방법으로 무엇을 사용하는가?
A) 조인
<!-- B) 정규화 -->
C) 인덱스
D) 트랜잭션

2.2. ACID 속성 중에서 '원자성'을 설명하는 것은?
A) 트랜잭션이 실행되는 동안 중간 상태를 허용하지 않는다.
<!-- B) 트랜잭션이 성공적으로 완료되지 않으면, 모든 변경 사항은 롤백된다. -->
C) 데이터베이스는 항상 일관된 상태를 유지한다.
D) 여러 트랜잭션이 동시에 실행될 수 있다.

2.3. SQL에서 'GROUP BY' 절의 주 용도는 무엇인가?
A) 데이터베이스의 데이터를 정렬하기 위해
B) 데이터를 특정 조건으로 필터링하기 위해
<!-- C) 데이터를 그룹화하여 집계 함수(Aggregate Function)를 적용하기 위해 -->
D) 데이터베이스의 테이블을 결합하기 위해

3. 네트워크
3.1. OSI 7 계층 중, 데이터 전송의 물리적인 매체와 전기적 신호를 담당하는 계층은 무엇인가?
A) 네트워크 계층
B) 전송 계층
<!-- C) 물리 계층 -->
D) 세션 계층

3.2. TCP/IP 프로토콜 중, 패킷을 보내기 전에 연결을 설정하고 신뢰성 있는 데이터 전송을 보장하는 프로토콜은 무엇인가?
A) UDP
B) HTTP
<!-- C) TCP -->
D) IP

3.3. DNS(Domain Name System)의 주 역할은 무엇인가?
A) IP 주소를 MAC 주소로 변환
<!-- B) 도메인 이름을 IP 주소로 변환 -->
C) 데이터를 패킷으로 나누어 전송
D) 클라이언트와 서버의 연결을 설정

4. 운영체제
4.1. 프로세스가 실행되기 전에 운영체제에서 할당하는 메모리 영역은 무엇인가?
A) 스택
<!-- B) 힙 -->
C) 코드 세그먼트
D) 데이터 세그먼트

4.2. 다음 중 교착상태(Deadlock)가 발생하지 않기 위한 조건이 아닌 것은 무엇인가?
A) 상호배제(Mutual Exclusion)
B) 점유와 대기(Hold and Wait)
C) 비선점(Non-preemption)
D) 순환대기(Circular Wait)

4.3. 운영체제에서 '문맥 교환(Context Switching)'의 주요 목적은 무엇인가?
A) 프로세스의 실행을 종료시키기 위해
<!-- B) 다중 프로세스가 CPU를 공평하게 사용할 수 있도록 하기 위해 -->
C) 시스템 자원을 모두 확보하기 위해
D) 데이터베이스에서 트랜잭션을 롤백하기 위해

4.4. 운영체제에서 '가상 메모리'의 가장 큰 장점은 무엇인가?
<!-- A) 물리 메모리보다 더 많은 프로그램을 실행할 수 있다. -->
B) CPU의 성능을 높인다.
C) 프로그램의 실행 속도를 높인다.
D) 네트워크 속도를 증가시킨다.

5. 기타
5.1. 인터럽트(Interrupt)와 관련된 설명으로 틀린 것은 무엇인가?
A) 인터럽트는 프로그램의 실행 흐름을 중단하고, 중요한 작업을 먼저 처리하게 한다.
B) 인터럽트는 하드웨어 또는 소프트웨어로 발생할 수 있다.
C) 인터럽트는 CPU의 시간 할당량을 관리하기 위해 사용된다.
<!-- D) 인터럽트는 프로세스가 실행 중일 때만 발생할 수 있다. -->

5.2. 버퍼(Buffer)의 주요 목적은 무엇인가?
A) 데이터를 암호화하기 위해
<!-- B) 데이터의 입출력을 효율적으로 처리하기 위해 -->
C) 데이터를 백업하기 위해
D) 데이터를 컴프레션하기 위해

5.3. 파이프라인(Pipeline) 기술의 주요 장점은 무엇인가?
A) 명령어 실행 시간을 줄일 수 있다.
B) 데이터베이스의 처리 속도를 증가시킬 수 있다.
<!-- C) 여러 명령어를 동시에 실행할 수 있다. -->
D) 시스템의 보안성을 강화할 수 있다.

1️⃣ 알고리즘 & 자료구조 (6문항)
Q1. 정렬 알고리즘 중 평균적인 시간 복잡도가 가장 빠른 것은?
① 선택 정렬
② 삽입 정렬
<!-- ③ 퀵 정렬 -->
④ 버블 정렬

Q2. BFS(너비 우선 탐색)와 DFS(깊이 우선 탐색)의 차이점으로 올바른 것은?
① BFS는 재귀를 사용하고 DFS는 큐를 사용한다.
<!-- ② BFS는 최단 경로 탐색에 유리하고 DFS는 모든 경로 탐색에 유리하다. -->
③ DFS는 최단 경로를 보장하지만 BFS는 보장하지 않는다.
④ BFS는 스택을 사용하고 DFS는 큐를 사용한다.

Q3. 힙(Heap)의 특징으로 올바른 것은?
① 모든 부모 노드는 두 개의 자식을 가져야 한다.
② 최소 힙에서는 부모 노드가 자식 노드보다 크다.
<!-- ③ 힙은 항상 이진 탐색 트리의 형태를 유지한다. -->
<!-- ④ 힙의 삽입/삭제 연산의 시간 복잡도는 O(log N)이다. -->

Q4. 동적 프로그래밍(Dynamic Programming)과 분할 정복(Divide and Conquer)의 차이점은?
<!-- ① 동적 프로그래밍은 동일한 문제를 여러 번 해결하며 캐싱을 활용한다. -->
② 분할 정복은 하위 문제를 중복해서 풀지만, 동적 프로그래밍은 중복을 피한다.
③ 동적 프로그래밍은 하향식(Top-Down) 방식만 사용된다.
④ 분할 정복은 테이블을 이용하여 값을 저장하며, 이를 통해 성능을 개선한다.

Q5. 해시 테이블에서 충돌(Collision)을 해결하는 방식이 아닌 것은?
① 체이닝(Chaining)
② 선형 탐사(Linear Probing)
<!-- ③ 이진 탐색(Binary Search) -->
④ 이중 해싱(Double Hashing)

Q6. 이진 탐색 트리(Binary Search Tree, BST)의 특징으로 틀린 것은?
① 모든 왼쪽 자식 노드는 부모보다 작고, 모든 오른쪽 자식 노드는 부모보다 크다.
② 중위 순회를 하면 오름차순으로 정렬된 결과가 나온다.
③ 균형이 맞지 않으면 검색 성능이 선형(O(N))이 될 수 있다.
<!-- ④ AVL 트리는 항상 완전 이진 트리의 형태를 유지한다. -->

2️⃣ 데이터베이스 (5문항)
Q7. 데이터베이스의 트랜잭션(Transaction) 특성이 아닌 것은?
① 원자성(Atomicity)
② 일관성(Consistency)
<!-- ③ 가변성(Variability) -->
④ 지속성(Durability)

Q8. 정규화(Normalization)의 목적이 아닌 것은?
① 중복 데이터를 제거하여 저장 공간을 절약한다.
② 데이터 무결성을 유지한다.
<!-- ③ 조인(Join) 연산을 최소화하여 성능을 향상시킨다. -->
④ 이상(Anomaly) 현상을 방지한다.

Q9. 데이터베이스 인덱스의 주요 기능으로 적절한 것은?
① 데이터베이스의 저장 공간을 줄인다.
<!-- ② 검색 성능을 향상시키지만, 삽입 및 삭제 성능을 저하시킬 수 있다. -->
③ 모든 컬럼에 인덱스를 설정하면 성능이 향상된다.
④ 인덱스가 있는 테이블은 항상 트랜잭션을 빠르게 수행한다.

Q10. 관계형 데이터베이스에서 JOIN 연산의 역할은?
① 테이블을 병합하여 중복 데이터를 제거하는 것
<!-- ② 두 개 이상의 테이블에서 관련된 데이터를 연결하는 것 -->
③ 특정 컬럼을 기준으로 데이터를 정렬하는 것
④ 데이터를 갱신(UPDATE)하는 것

Q11. NoSQL 데이터베이스의 특징으로 틀린 것은?
① 스키마가 고정되지 않아 유연한 데이터 저장이 가능하다.
② 수직적 확장이 아닌 수평적 확장이 용이하다.
<!-- ③ 관계형 데이터베이스보다 무결성 제약이 강하다. -->
④ 키-값 저장소, 컬럼 지향 데이터베이스, 문서 저장소 등 다양한 모델이 존재한다.

3️⃣ 네트워크 (5문항)
Q12. OSI 7계층에서 '라우터(Router)'가 동작하는 계층은?
① 물리 계층(Physical Layer)
② 데이터 링크 계층(Data Link Layer)
<!-- ③ 네트워크 계층(Network Layer) -->
④ 전송 계층(Transport Layer)

Q13. TCP와 UDP의 차이점으로 올바른 것은?
① TCP는 비연결형 프로토콜이고, UDP는 연결형 프로토콜이다.
② UDP는 신뢰성이 보장되지만, TCP는 신뢰성이 보장되지 않는다.
<!-- ③ TCP는 흐름 제어 및 혼잡 제어를 제공하지만, UDP는 제공하지 않는다. -->
④ UDP는 데이터가 손실될 가능성이 없으며, TCP보다 빠르다.

Q14. HTTP와 HTTPS의 차이점으로 틀린 것은?
① HTTPS는 SSL/TLS 프로토콜을 사용하여 데이터를 암호화한다.
② HTTPS는 HTTP보다 보안성이 높다.
<!-- ③ HTTP는 기본적으로 443번 포트를 사용한다. -->
④ HTTPS는 TLS 인증서를 필요로 한다.

Q15. 서브넷 마스크(Subnet Mask)의 역할은?
① 패킷을 암호화하는 기능을 수행한다.
<!-- ② IP 주소를 네트워크 주소와 호스트 주소로 구분한다. -->
③ 데이터를 압축하여 전송 속도를 높인다.
④ TCP 연결을 종료하는 역할을 한다.

Q16. NAT(Network Address Translation)의 주요 기능은?
<!-- ① 내부 네트워크의 IP 주소를 공인 IP 주소로 변환한다. -->
② 패킷의 데이터 암호화를 수행한다.
③ 네트워크 장비 간의 충돌을 방지한다.
④ 전송 계층의 포트를 관리하는 역할을 한다.

4️⃣ 운영체제 (4문항)
Q17. 프로세스와 스레드의 차이점으로 올바른 것은?
① 프로세스는 CPU에서 실행되는 단위이며, 스레드는 운영체제에서 실행되는 단위이다.
<!-- ② 프로세스는 독립적인 메모리 공간을 가지며, 스레드는 메모리를 공유한다. -->
③ 스레드는 독립적으로 실행되지만, 프로세스는 그렇지 않다.
④ 프로세스는 공유 메모리를 사용하지만, 스레드는 그렇지 않다.

Q18. CPU 스케줄링 알고리즘 중 '비선점형(Non-preemptive)' 방식이 아닌 것은?
<!-- ① FCFS(First Come First Serve) -->
② SJF(Shortest Job First)
③ Round Robin
④ Priority Scheduling (우선순위 스케줄링)

Q19. 데드락(Deadlock) 발생 조건이 아닌 것은?
① 상호 배제(Mutual Exclusion)
② 비선점(No Preemption)
③ 환형 대기(Circular Wait)
<!-- ④ 선점 가능(Preemptive) -->

Q20. 가상 메모리(Virtual Memory)의 주요 개념이 아닌 것은?
① 페이지 교체(Page Replacement)
② 스왑(Swapping)
<!-- ③ 캐싱(Caching) -->
④ 디스크 조각 모음(Defragmentation)

Q1. 다음 중 선점형 스케줄링 기법이 아닌 것은?
(A) 다단계 큐(Multilevel Queue)
(B) 라운드 로빈(Round Robin)
<!-- (C) 최단 작업 우선(SJF, Shortest Job First) -->
(D) 선점형 우선순위 스케줄링(Preemptive Priority Scheduling)

Q2. 프로세스가 CPU를 사용하다가 I/O 작업을 위해 중단된 경우, 다음 중 선점형/비선점형 방식에서의 차이점이 아닌 것은?
(A) 선점형에서는 다른 프로세스가 즉시 CPU를 사용할 수 있다.
(B) 비선점형에서는 해당 프로세스가 I/O 작업이 끝날 때까지 CPU가 낭비된다.
(C) 선점형 스케줄링에서는 우선순위가 낮은 프로세스는 CPU를 사용하지 못할 수도 있다.
<!-- (D) 비선점형 스케줄링에서도 우선순위가 높은 프로세스가 들어오면 현재 프로세스를 중단시킨다. -->

Q3. 다음 중 다중 프로세스 환경에서 선점형 스케줄링을 사용할 때 발생할 수 있는 주요 문제점은?
(A) 우선순위 역전(Priority Inversion) <!-- 우선 순위가 낮은 프로세스가 자원을 점유하여 우선 순위가 높은 프로세스는 스케줄링을 받지 못하고 뒤로 밀리는 역전 현상이 발생 가능, 우선 순위 상속으로 해결 -->
(B) 기아 현상(Starvation) <!-- 선점형 우선순위 큐에서 우선 순위가 낮은 프로세스에서 발생 가능 -->
(C) 문맥 교환(Context Switching) 오버헤드
<!-- (D) 모든 항목이 해당됨 -->

Q4. 가상 메모리 시스템에서 페이지 교체(Page Replacement) 기법이 필요한 이유로 적절하지 않은 것은?
(A) 메모리가 부족할 때 새로운 페이지를 로드하기 위해 필요하다.
<!-- (B) 프로세스의 메모리 접근 속도를 높이기 위해 필요하다. -->
(C) 물리 메모리(RAM)보다 많은 프로그램을 실행할 수 있도록 하기 위해 필요하다.
(D) 페이지 폴트(Page Fault)가 발생했을 때 적절한 페이지를 선택하여 제거하기 위해 필요하다.

Q5. 페이지 교체 알고리즘 중 OPT(Optimal Page Replacement) 기법이 실무에서 사용되지 않는 이유는?
<!-- (A) 미래의 메모리 접근 패턴을 미리 알기 어렵기 때문이다. -->
(B) 페이지 교체를 위한 추가적인 하드웨어가 필요하기 때문이다.
(C) FIFO보다 페이지 폴트가 많이 발생하기 때문이다.
(D) LRU(Least Recently Used)보다 성능이 항상 낮기 때문이다.

Q6. 다음 중 데드락이 발생하지 않도록 하는 예방 기법(Deadlock Prevention)이 아닌 것은?
(A) 프로세스가 자원을 요청할 때, 한 번에 모든 필요한 자원을 할당한다.
<!-- (B) 프로세스가 필요한 자원을 사용할 수 없으면 즉시 종료시킨다. -->
(C) 프로세스가 일정 시간 안에 자원을 사용하지 않으면 회수한다.
(D) 자원 할당 순서를 미리 정하고, 모든 프로세스가 그 순서를 따르도록 한다.

Q7. 데드락 회피(Avoidance) 알고리즘 중 은행원 알고리즘(Banker's Algorithm)에 대한 설명으로 올바른 것은?
<!-- (A) 모든 프로세스가 필요한 최대 자원을 미리 알려야 한다. -->
(B) 데드락이 발생했을 때 자동으로 해결하는 알고리즘이다.
(C) 자원 할당 순서를 랜덤하게 결정하여 데드락을 방지한다.
(D) FIFO 스케줄링과 함께 사용해야만 효과를 볼 수 있다.

Q8. 다음 중 자원 할당 그래프(Resource Allocation Graph)를 활용한 데드락 탐지(Detection)에 대한 설명으로 올바르지 않은 것은?
(A) 사이클(Cycle)이 존재하면 데드락 상태일 가능성이 높다.
(B) 자원 할당 그래프에서 사이클이 없으면 데드락이 발생하지 않는다.
<!-- (C) 탐지 알고리즘은 모든 프로세스를 종료시키고 다시 시작해야 한다. -->
(D) 데드락 탐지는 CPU 오버헤드가 발생할 수 있다.

Q9. 다음은 여러 프로세스가 실행 중인 시스템에서 스케줄링과 데드락 방지 기법을 조합하는 방식이다. 올바르지 않은 조합을 고르시오.
<!-- (A) 라운드 로빈(RR) + 선점형 우선순위 스케줄링 → 실행 시간이 짧은 작업을 먼저 실행 -->
(B) 은행원 알고리즘(Banker's Algorithm) + 자원 할당 순서 제한 → 안전한 상태 유지
(C) 페이징 기법 + LRU 페이지 교체 → 가상 메모리 성능 최적화
<!-- (D) 우선순위 스케줄링 + 선점 불가능(Non-preemptive) → 기아 현상을 줄이기 위한 최적 조합 -->

Q1. 다음 중 평균적인 시간 복잡도가 가장 빠른 정렬 알고리즘은?
(A) 힙 정렬 (Heap Sort)
(B) 병합 정렬 (Merge Sort)
(C) 퀵 정렬 (Quick Sort)
<!-- (D) 기수 정렬 (Radix Sort) -->

Q2. 다음 중 탐색 연산의 시간 복잡도가 가장 낮을 수 있는 자료구조는?
<!-- (A) 해시 테이블 (Hash Table) -->
(B) 이진 탐색 트리 (Binary Search Tree)
(C) AVL 트리
(D) 스택

Q3. 다익스트라 알고리즘과 벨만 포드 알고리즘의 차이점으로 올바른 것은?
(A) 다익스트라는 음수 가중치를 지원하지만 벨만 포드는 지원하지 않는다.
(B) 벨만 포드는 모든 간선을 한 번만 검사하지만, 다익스트라는 여러 번 검사한다.
<!-- (C) 벨만 포드는 음수 가중치를 지원하지만, 다익스트라는 지원하지 않는다. -->
(D) 두 알고리즘 모두 음수 가중치를 지원한다.

Q4. 세그먼트 트리(Segment Tree)의 주요 특징으로 옳지 않은 것은?
(A) 세그먼트 트리는 O(log N) 시간 복잡도로 구간 합을 구할 수 있다.
(B) 업데이트 연산은 O(log N)으로 가능하다.
(C) 기본적으로 트리 구조를 사용하며, 배열을 기반으로 구현될 수 있다.
<!-- (D) 모든 노드는 단일 원소만 저장할 수 있다. -->

Q5. KMP(Knuth-Morris-Pratt) 문자열 검색 알고리즘에 대한 설명으로 틀린 것은?
(A) 패턴 내의 중복 정보를 이용하여 불필요한 비교를 줄인다.
(B) 최악의 경우에도 O(N) 시간 복잡도를 보장한다.
(C) 전처리 과정에서 LPS(Longest Prefix Suffix) 배열을 생성한다.
<!-- (D) 브루트포스보다 항상 빠르게 동작한다. -->

Q6. 힙(Heap)과 우선순위 큐(Priority Queue)의 관계로 올바른 것은?
<!-- (A) 힙은 반드시 완전 이진 트리 형태를 유지해야 한다. -->
(B) 우선순위 큐는 힙을 기반으로 구현될 필요가 없다.
(C) 힙의 삽입 연산의 평균 시간 복잡도는 O(N)이다.
(D) 힙은 항상 이진 탐색 트리의 형태를 유지한다.

2️⃣ 데이터베이스 (4문항)
Q7. 인덱스를 사용할 때 성능이 저하될 수 있는 경우는?
(A) 테이블이 너무 클 때
(B) WHERE 절에 인덱스가 설정되지 않은 컬럼이 포함될 때
<!-- (C) 자주 변경되는 컬럼에 인덱스를 설정할 때 -->
(D) 모든 항목이 해당됨

Q8. 관계형 데이터베이스의 정규화 과정에서 3NF(제3정규형)를 만족하기 위한 조건은?
<!-- (A) 모든 속성이 기본키에 직접 종속되어야 한다. -->
(B) 모든 속성이 기본키가 아닌 속성에 의존해야 한다.
(C) 기본키가 복합키일 경우, 일부 속성만 의존해도 된다.
(D) 함수적 종속성을 유지해야 한다.

Q9. NoSQL의 주요 특징으로 올바르지 않은 것은?
(A) 수직적 확장보다 수평적 확장에 초점을 둔다.
<!-- (B) 정규화를 수행하여 데이터 중복을 최소화한다. -->
(C) 스키마가 유연하다.
(D) 키-값 저장소, 컬럼형 데이터베이스 등의 다양한 유형이 존재한다.

Q10. 트랜잭션 격리 수준 중 가장 높은 수준(강력한 격리 보장)을 제공하는 것은?
(A) READ UNCOMMITTED
(B) READ COMMITTED
(C) REPEATABLE READ
<!-- (D) SERIALIZABLE -->

3️⃣ 네트워크 (4문항)
Q11. TCP의 혼잡 제어 기법 중 ‘느린 시작(Slow Start)’의 설명으로 올바른 것은?
(A) 네트워크의 상태를 확인한 후 데이터 전송을 시작한다.
<!-- (B) 처음에는 작은 윈도우 크기로 전송을 시작하고 점진적으로 증가시킨다. -->
(C) 혼잡이 감지되면 즉시 윈도우 크기를 0으로 만든다.
(D) ACK 패킷이 오지 않으면 타이머를 초기화한다.

Q12. 서브넷 마스크(Subnet Mask)의 역할로 올바른 것은?
<!-- (A) IP 주소를 호스트와 네트워크 부분으로 분리한다. -->
(B) 네트워크 장비 간의 충돌을 방지한다.
(C) 데이터 패킷을 암호화하는 역할을 한다.
(D) TCP 3-Way Handshake 과정에서 사용된다.

Q13. HTTP와 HTTPS의 차이점 중 틀린 것은?
(A) HTTPS는 TLS/SSL을 이용하여 데이터를 암호화한다.
(B) HTTPS는 기본적으로 443번 포트를 사용한다.
<!-- (C) HTTP는 비연결형 프로토콜이다. -->
(D) HTTPS는 MITM(중간자 공격)에 대한 보안성이 높다.

Q14. DNS의 역할로 올바르지 않은 것은?
(A) 도메인 이름을 IP 주소로 변환한다.
(B) DNS는 계층적 구조로 이루어져 있다.
<!-- (C) 모든 DNS 요청은 루트 네임서버를 거쳐야 한다. -->
(D) 캐싱을 사용하여 반복 요청을 빠르게 처리할 수 있다.

4️⃣ 운영체제 (4문항)
Q15. 다음 중 스레드(Thread)에 대한 설명으로 옳지 않은 것은?
(A) 스레드는 프로세스 내에서 실행되는 최소 실행 단위이다.
(B) 동일 프로세스 내 스레드는 메모리 공간을 공유한다.
<!-- (C) 각 스레드는 고유한 힙 메모리를 가진다. -->
(D) 스레드 간의 자원 공유는 병렬 처리에서 효율적일 수 있다.

Q16. 데드락이 발생하는 4가지 조건 중 ‘점유와 대기(Hold and Wait)’을 방지하는 방법으로 적절한 것은?
(A) 자원이 사용 가능할 때까지 대기 큐에 넣는다.
<!-- (B) 프로세스가 필요한 모든 자원을 한 번에 요청하고, 모두 할당되지 않으면 아무것도 받지 않는다. -->
(C) 자원이 부족할 때 프로세스를 강제 종료한다.
(D) 자원을 요청하기 전, 운영체제의 허가를 받아야 한다.

Q17. 페이징 기반 가상 메모리 시스템에서 ‘페이지 폴트(Page Fault)’가 발생하는 경우는?
(A) 프로세스가 허용되지 않은 주소에 접근할 때
(B) 필요한 페이지가 이미 메모리에 존재할 때
<!-- (C) 접근한 페이지가 메모리에 존재하지 않아 디스크에서 로드해야 할 때 -->
(D) 캐시가 가득 찼을 때

Q18. 프로세스 상태 전이(Process State Transition) 중 올바른 전이로만 이루어진 것은?
<!-- (A) Ready → Running → Waiting → Terminated -->
(B) New → Ready → Running → Terminated
(C) Running → Waiting → New → Ready
(D) Waiting → Ready → New → Running

Q19. RESTful API 설계 원칙에 대한 설명 중 올바르지 않은 것은?
(A) 리소스는 URI로 표현되며, 동사는 HTTP 메서드로 표현된다.
<!-- (B) 서버는 상태를 유지하며 클라이언트의 이전 요청 정보를 활용해야 한다. -->
(C) POST는 주로 새로운 리소스를 생성할 때 사용된다.
(D) GET 요청은 데이터를 변경하지 않아야 하며, 안전해야 한다.

Q20. 블록체인 기술의 '합의 알고리즘(Consensus Algorithm)'이 사용되는 주요 이유는?
(A) 데이터를 암호화하기 위해
(B) 탈중앙화를 방지하기 위해
<!-- (C) 네트워크 참여자 간 동일한 거래 기록을 보장하기 위해 -->
(D) 스마트 컨트랙트를 자동으로 실행하기 위해

1. [알고리즘] 위상 정렬(Topological Sort)에 대한 설명으로 옳지 않은 것은?
(A) DAG(사이클이 없는 방향 그래프)에만 적용할 수 있다.
(B) 진입 차수가 0인 정점을 반복적으로 제거하며 정렬한다.
<!-- (C) 위상 정렬의 결과는 항상 유일하다. -->
(D) 사이클이 존재하면 위상 정렬이 불가능하다.

2. [자료구조] 다음 중 트라이(Trie)에 대한 설명으로 올바른 것은?
(A) 모든 노드는 알파벳 개수만큼 자식을 가진다.
(B) 트라이는 검색 속도가 O(log N)이다.
(C) 문자열의 공통 접두사를 효율적으로 저장하는 데 사용된다.
<!-- (D) 메모리 사용량이 낮기 때문에 큰 데이터를 저장하기 적합하다. -->

3. [운영체제] 페이징과 세그멘테이션을 혼합한 메모리 관리 기법의 특징은?
(A) 물리 메모리를 연속적으로 할당하여 단편화를 방지한다.
<!-- (B) 가상 주소를 세그먼트와 페이지 번호로 나누어 접근한다. -->
(C) 내부 단편화를 해결하기 위해 사용된다.
(D) 모든 프로그램은 하나의 세그먼트로만 구성된다.

4. [운영체제] 다음 중 교착 상태(Deadlock) 예방 기법으로 가장 적절한 것은?
(A) 자원을 사용한 후 일정 시간 안에 자동 회수
<!-- (B) 자원을 요청할 때, 필요한 모든 자원을 한 번에 요청 -->
(C) 자원이 부족하면 프로세스를 대기 큐에 넣음
(D) 자원을 선점할 수 없도록 설정

5. [네트워크] TCP와 UDP 모두 사용하는 프로토콜은 무엇인가?
(A) HTTP
<!-- (B) FTP -->
(C) DNS
(D) SSH

6. [네트워크] HTTP/2의 주요 특징이 아닌 것은?
<!-- (A) 하나의 TCP 연결에서 여러 요청을 동시에 보낼 수 있다. -->
(B) 요청과 응답은 바이너리 프레이밍을 사용한다.
(C) 서버는 클라이언트 요청이 없어도 데이터를 푸시할 수 있다.
(D) 요청 순서가 보장되므로 HOL(Head of Line) 블로킹이 발생하지 않는다.

7. [데이터베이스] 뷰(View)에 대한 설명 중 옳지 않은 것은?
(A) 뷰는 가상의 테이블로, 실제 데이터를 저장하지 않는다.
<!-- (B) 뷰를 통해 SELECT는 가능하지만 INSERT는 절대 불가능하다. -->
(C) 보안 목적으로 사용 가능하다.
(D) 복잡한 쿼리를 단순화할 수 있다.

8. [데이터베이스] 트랜잭션에서 연쇄 복구(Cascading Rollback)가 발생하지 않도록 보장하는 격리 수준은?
(A) Read Uncommitted
(B) Read Committed
<!-- (C) Repeatable Read -->
(D) Serializable

9. [보안/네트워크] TLS의 Handshake 단계에서 일어나는 일이 아닌 것은?
(A) 클라이언트와 서버가 서로 지원하는 암호 스위트를 교환
(B) 서버가 공개키를 통해 대칭키를 암호화하여 클라이언트에 전달
(C) 클라이언트는 서버의 인증서를 검증
<!-- (D) 클라이언트는 서버에게 MAC 키를 직접 전달 -->

10. [API 설계] RESTful API에서 다음 URI 중 리소스 표현이 비RESTful한 것은?
(A) GET /users/123
<!-- (B) DELETE /products/567 -->
(C) POST /login
(D) GET /getAllUsers